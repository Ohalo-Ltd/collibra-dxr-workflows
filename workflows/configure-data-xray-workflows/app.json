{
  "key": "configureDataXrayWorkflows",
  "name": "Configure Data X-Ray Workflows",
  "description": "One-time setup, run by an admin: creates the Data X-Ray operating model (community, domains, asset types, attribute types, assignments) that the Data X-Ray sync & search workflows reference by fixed ID. Idempotent — safe to re-run. You still set each workflow's Base URL and Bearer token by hand.",
  "theme": "theme-2",
  "icon": "glyphicon-cog",
  "businessItemResourceType": "GLOBAL",
  "globalCreate": true
}
